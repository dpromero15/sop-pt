# OKRs — v2.1.0 Session activities and sideline logger

**Cycle:** v2.1.0  
**Team:** Soccer Team Manager

## Objective

Make session logging sideline-fast: every session owns its metrics (attendance defaulted), attendance is swipe-driven, and scoring only shows players who showed up.

## Key Results

| ID | Key Result | Evidence | Score |
|---|---|---|---|
| KR1 | Create session always attaches attendance; coach can add measured/game metrics to `Session.metricIds` | Session type + planner UI + storage | 1.0 |
| KR2 | Attendance uses swipe deck (R present / L absent / Down excused) with long-press LATE stamp toggle | AttendanceSwipeDeck | 1.0 |
| KR3 | Metric scoring uses one-player card deck filtered to present+late only; session→logger handoff passes `sessionId` | PlayerScoreCard + App handoff | 1.0 |
| KR4 | `docs/` includes v2.1 OKRs + session-logging SOP + storage contract; GitHub `v2.1.0` issues cover planned epics | docs/ + GitHub issues | 1.0 |

## Scoring guide (Google)

- **1.0** — Fully achieved
- **0.6–0.7** — Target zone for stretch work
- **Below 0.4** — Root-cause review required

Ship v2.1 when average KR score ≥ **0.7**.

## End-of-cycle notes

- What drove results?
- What would we do differently?
- What rolls forward vs. closes?
