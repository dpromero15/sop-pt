# OKRs — v2.0.0 Team management and durable storage

**Cycle:** v2.0.0  
**Team:** Soccer Team Manager

## Objective

Make Soccer Team Manager a real team tool with editable team identity and durable, portable storage.

## Key Results

| ID | Key Result | Evidence | Score |
|---|---|---|---|
| KR1 | Admin can view/edit team profile (name + core fields) and see changes persist across reload in cloud mode | Team Management UI + local/cloud persistence | 0.8 |
| KR2 | Storage uses ≥6 separated collections/blobs (`team`, `players`, `sessions`, `entries`, `metrics`, `labels`, `formula`); no single dump document for runtime | storage contracts + LocalJsonAdapter keys | 1.0 |
| KR3 | Offline/unconfigured mode falls back to local JSON blobs with visible connection status in Admin tools | Admin tools mode badge `cloud` \| `local-fallback` | 0.9 |
| KR4 | `docs/` published with OKRs, architecture ADRs, and API/data contracts; GitHub issues cover 100% of planned epics | docs/ + GitHub `v2.0.0` issues | 1.0 |

## Scoring guide (Google)

- **1.0** — Fully achieved
- **0.6–0.7** — Target zone for stretch work
- **Below 0.4** — Root-cause review required

Ship v2 when average KR score ≥ **0.7**.

## End-of-cycle notes

- What drove results?
- What would we do differently?
- What rolls forward vs. closes?
